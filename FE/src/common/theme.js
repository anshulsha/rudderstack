import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  spacing: 8, // Adjust the spacing value according to your needs
  // ... Other theme options
});

export default theme;
